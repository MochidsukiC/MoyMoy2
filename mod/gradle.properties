org.gradle.jvmargs=-Xmx3G
org.gradle.daemon=false

# Minecraft / Forge — pinned to the 1.20.1 line to match the MochiOS connector
# mod (mochi). Same MDK shape as MochiOS2.0/minecraft/forge.
minecraft_version=1.20.1
minecraft_version_range=[1.20.1,1.21)
forge_version=47.4.0
forge_version_range=[47,)
loader_version_range=[47,)

# Official Mojang mappings — match the connector mod.
mapping_channel=official
mapping_version=1.20.1

# Mod identity. The connector app_id is also "moymoy".
mod_id=moymoy
mod_name=MoyMoy
mod_license=Proprietary
mod_version=0.1.0
mod_group_id=jp.houlab.mochidsuki.moymoy
mod_authors=Mochidsuki
mod_description=MoyMoy — MochiOS in-world emerald wallet (DEV.md 7.3). A connector receiver extension that consumes a player's inventory emeralds to fund their moymoy.cs.mnn wallet balance, driven by the wallet backend over the command bus.
